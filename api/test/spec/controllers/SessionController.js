'use strict';

var chai = require('chai')
  , should = chai.should();

describe('SessionController',function() {
	describe('#login',function() {
		it('should check the data store for a valid user and return any feedback');
	});
});